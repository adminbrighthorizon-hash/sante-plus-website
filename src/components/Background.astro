<div class="pointer-events-none fixed inset-0 z-[-2] overflow-hidden bg-white">
  <div class="absolute inset-0 bg-[radial-gradient(90%_55%_at_50%_0%,rgba(167,243,208,0.45)_0,rgba(249,168,212,0.18)_52%,rgba(255,255,255,0)_100%)]"></div>
  <div class="aurora orb-one"></div>
  <div class="aurora orb-two"></div>
  <div class="aurora orb-three"></div>
  <div class="grid-fade"></div>
</div>

<style>
  .aurora {
    position: absolute;
    border-radius: 9999px;
    filter: blur(24px);
    opacity: 0.55;
    will-change: transform;
    animation: float 14s ease-in-out infinite;
  }

  .orb-one {
    width: 24rem;
    height: 24rem;
    top: -6rem;
    left: -5rem;
    background: linear-gradient(140deg, rgba(16, 185, 129, 0.45), rgba(14, 116, 144, 0.15));
  }

  .orb-two {
    width: 22rem;
    height: 22rem;
    right: -4rem;
    top: 24%;
    background: linear-gradient(140deg, rgba(244, 114, 182, 0.36), rgba(251, 191, 36, 0.16));
    animation-delay: -4s;
  }

  .orb-three {
    width: 18rem;
    height: 18rem;
    left: 28%;
    bottom: -5rem;
    background: linear-gradient(140deg, rgba(59, 130, 246, 0.28), rgba(34, 197, 94, 0.14));
    animation-delay: -8s;
  }

  .grid-fade {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(to right, rgba(148, 163, 184, 0.09) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(148, 163, 184, 0.09) 1px, transparent 1px);
    background-size: 48px 48px;
    mask-image: radial-gradient(65% 55% at 50% 40%, black 40%, transparent 100%);
    opacity: 0.35;
  }

  @keyframes float {
    0%,
    100% {
      transform: translate3d(0, 0, 0) scale(1);
    }
    50% {
      transform: translate3d(24px, -18px, 0) scale(1.08);
    }
  }
</style>
